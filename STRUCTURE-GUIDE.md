CSS STRUCTURE:
styles/
├── variables.css    → all :root colors, fonts, spacing
├── reset.css        → resets, defaults, base body styles
├── components.css   → all section styles, cards, buttons
└── responsive.css   → media queries, mobile first


JS STRUCTURE:
js/
├── nav.js           → scroll effect + mobile menu
├── reveal.css       → scroll reveal animation
├── counter.js       → stat number counter
└── utils.js         → wallet copy + photo fallback